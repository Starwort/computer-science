---
layout: default
title: page135questions | Computer Science
footer: Preprocessed by AutoDocs.preprocess 2.0.0 "clean rewrite and preprocessing" ⓒ Starwort, 2020
---

1)
  a)
    Species [red in colour]

    There are over 100 species of rose.

    - Climbing roses
    - Shrub roses
    - Rambling roses
  b) <a href="http://www.roses.com"><img src="./rose.jpg"></a>
  c)
    i) a full stylesheet is not needed
   ii)
      - a consistent style can be applied over multiple pages
      - the html is cleaner
  d)
    i) makes the page's background light green
   ii) makes all p tags with the classname 'bold' bold
    i) h1 tags are aligned centrally
  e) by using the css selector 'ul' to set the property 'color' to 'green'
2)
  a) .classes target elements with the 'class' attribute set, for example 'div.content' targets a 'div' tag with the class 'content'; whereas #identifiers target elements with the tag 'id' set - for example 'p#licence' targets a 'p' tag with id 'licence'
  b) pseudoclasses or multiple selectors may be used to change the scope of the css selector, for example, limiting it to only a cetain tag, class, or id
